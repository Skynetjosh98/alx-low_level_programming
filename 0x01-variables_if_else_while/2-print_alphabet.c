#include <stdio.h>

/**
 * main - script that prints the alphabet in lowercase,
 * followed by a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alp;

	for (alp = 'a'; ch <= 'z'; ch++)
	{
		putchar(alp);
	}
	putchar('\n');
	return (0);
}
